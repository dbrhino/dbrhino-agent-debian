# Defaults for dbrhino-agent initscript
# sourced by /etc/init.d/dbrhino-agent
# installed at /etc/default/dbrhino-agent by the maintainer scripts

#
# This is a POSIX shell fragment
#


# Change the following for your config to be in a different
# location
# DBRHINO_AGENT_CONFIG_DIR=/etc/dbrhino-agent

# Change to send the logs to a different location
# DBRHINO_AGENT_LOG_PATH=/var/log/dbrhino-agent.log
